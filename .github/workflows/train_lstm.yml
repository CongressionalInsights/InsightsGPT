name: Train LSTM Model
on: workflow_dispatch

jobs:
  run:
    name: Run LSTM Model
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@23
full
      - name: Validate Datasev‚@go havs to e a valid validation tested creatograms
