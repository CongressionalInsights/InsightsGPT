name: Train LSTM Model
on:
  workflow_dispatch
jobs:
  run:
    name: Run LSTM Model
    runs-on: ubuntu-latest
    steps: 
      - name: Check out repository
        uses: actions/checkout@3
      - name: Validate Dataset
        run: |
          if [ -f "data/preprocessed_bill_data.csv" ]; then
            chose train_script exit Subresence shell
if=ran matches

