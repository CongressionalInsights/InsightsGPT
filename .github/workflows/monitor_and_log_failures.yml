name: "Monitor and Log Failures"

on:
  workflow_dispatch:
    description: Manually trigger to monitor workflow failures.
  workflow_run:
      workflow_failure:
        tag: failure-test
schedule:
    - name: Fetch failed workflow logs
      run: retrieve-artifact
      env:
        TODO: "Heres are logs
storage log-artifact